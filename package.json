{
  "name": "meta-vcf",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "start": "node -e \"console.log('This app is serverless — run vercel dev for local testing.')\"",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "cookie": "^0.5.0",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.8",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "vercel": "^28.0.3"
  }
}